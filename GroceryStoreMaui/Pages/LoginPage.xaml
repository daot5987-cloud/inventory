<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GroceryStoreMaui.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Đăng nhập"
    BackgroundColor="#F3F5F9">

    <ScrollView>
        <Grid Padding="24">

            <!-- Căn giữa form -->
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- FORM LOGIN -->
            <Frame
                Grid.Row="1"
                Padding="24"
                CornerRadius="24"
                HasShadow="True"
                BackgroundColor="White"
                HorizontalOptions="Center"
                WidthRequest="600">

                <VerticalStackLayout Spacing="18">

                    <!-- Tiêu đề đẹp hơn -->
                    <HorizontalStackLayout Spacing="8" HorizontalOptions="Center">
                        <Label Text="🛒" FontSize="32" />
                        <Label
                            Text="Quản lý cửa hàng tạp hóa"
                            FontSize="22"
                            FontAttributes="Bold"
                            VerticalTextAlignment="Center" />
                    </HorizontalStackLayout>

                    <Label
                        Text="Đăng nhập để tiếp tục"
                        FontSize="13"
                        HorizontalOptions="Center"
                        TextColor="Gray" />

                    <!-- Username -->
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Tài khoản" FontSize="13" TextColor="#555" />
                        <Entry
                            x:Name="UsernameEntry"
                            Placeholder="Nhập tài khoản..." />
                    </VerticalStackLayout>

                    <!-- Password -->
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Mật khẩu" FontSize="13" TextColor="#555" />
                        <Entry
                            x:Name="PasswordEntry"
                            Placeholder="Nhập mật khẩu..."
                            IsPassword="True" />
                    </VerticalStackLayout>

                    <!-- Login button -->
                    <Button
                        Text="Đăng nhập"
                        HeightRequest="44"
                        CornerRadius="22"
                        FontAttributes="Bold"
                        Clicked="OnLoginClicked"
                        BackgroundColor="#1976D2"
                        TextColor="White" />

                    <Label
                        Text="Tài khoản mặc định: admin / 123456"
                        FontSize="12"
                        HorizontalTextAlignment="Center"
                        TextColor="Gray"
                        Margin="0,8,0,0" />

                </VerticalStackLayout>

            </Frame>

        </Grid>
    </ScrollView>

</ContentPage>
